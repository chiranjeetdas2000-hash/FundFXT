// FundFXT runtime patch: canonical live account/evaluation payload.
const fs = require('fs');
const originalRead = fs.readFileSync;
fs.readFileSync = function(file, encoding, ...rest) {
  const value = originalRead.call(this, file, encoding, ...rest);
  if (typeof file === 'string' && /backend[\\/]server\.js$/.test(file) && typeof value === 'string') {
    const route = `app.get('/api/accounts',authenticateToken,async(req,res)=>{try{const[accounts]=await db.execute('SELECT * FROM accounts WHERE user_id = ?',[req.userId]);const normalized=[];for(const account of accounts){const model=String(account.challenge_model||'').toLowerCase();let config={};try{const[cfg]=await db.execute('SELECT * FROM challenge_configs WHERE model_key = ? LIMIT 1',[model]);config=cfg[0]||{}}catch(_){}const n=v=>v===null||v===undefined||v===''?null:Number(v),pick=(...keys)=>{for(const k of keys){if(config[k]!==null&&config[k]!==undefined&&config[k]!=='')return config[k];if(account[k]!==null&&account[k]!==undefined&&account[k]!=='')return account[k]}return null},initial=n(account.initial_balance_cents)??n(account.balance_cents)??0,balance=n(account.balance_cents)??0,dayStart=n(account.day_start_balance_cents)??balance;let openTrades=[];try{[openTrades]=await db.execute("SELECT * FROM trades WHERE account_id=? AND status='OPEN'",[account.id])}catch(_){}let floatingCents=0;for(const tr of openTrades){const q=global.priceCache?.[tr.symbol];if(!q)continue;const px=String(tr.side).toUpperCase()==='BUY'?Number(q.bid):Number(q.ask);if(!Number.isFinite(px))continue;try{floatingCents+=Math.round(calculatePL(tr.symbol,tr.side,Number(tr.entry_price),px,Number(tr.volume))*100)}catch(_){}}const equity=balance+floatingCents;const direct=model==='prototype_5k',warrior=model==='warrior_5k';const phase=String(pick('phase','stage','account_phase','challenge_phase')||(warrior?'PHASE_1':direct?'DIRECT_FUNDED':'—'));const targetBps=n(pick('profit_target_bps','target_bps','profit_target_percent_bps','profit_target_basis_points')),targetPct=n(pick('profit_target_percent','target_percent','profit_target_pct','target_pct'));const targetCents=n(pick('profit_target_cents','target_profit_cents','target_cents'))??(targetBps!=null?Math.round(initial*targetBps/10000):targetPct!=null?Math.round(initial*targetPct/100):null);const dailyBps=n(pick('daily_dd_bps')),maxBps=n(pick('max_dd_bps')),dailyLimit=dailyBps==null?null:Math.round(dayStart*dailyBps/10000),maxLimit=maxBps==null?null:Math.round(initial*maxBps/10000),dailyUsed=Math.max(0,dayStart-balance),maxUsed=Math.max(0,initial-equity);const consistencyBps=n(pick('consistency_bps','consistency_rule_bps','consistency_percent_bps')),consistencyPct=n(pick('consistency_percent','consistency_pct','consistency_rule_percent'));let bestDay=0,totalProfit=0;try{const[days]=await db.execute("SELECT trading_day,SUM(CASE WHEN realized_profit_cents>0 THEN realized_profit_cents ELSE 0 END) AS day_profit FROM trades WHERE account_id=? AND status='CLOSED' GROUP BY trading_day",[account.id]);for(const d of days){const v=Math.max(0,Number(d.day_profit||0));totalProfit+=v;if(v>bestDay)bestDay=v}}catch(_){}const consistencyCurrent=totalProfit>0?bestDay/totalProfit*100:0;const consistencyLimit=consistencyBps!=null?consistencyBps/100:consistencyPct;let tradesToday=0;try{const[t]=await db.execute("SELECT COUNT(*) AS count FROM trades WHERE account_id=? AND trading_day=CURDATE()",[account.id]);tradesToday=Number(t[0]?.count||0)}catch(_){}const maxTrades=n(pick('max_trades_per_day','max_daily_trades'))??(warrior?3:null);const accountType=String(pick('account_type')||(direct?'Direct Funded Account':warrior?'Warrior Account':'Trading Account'));normalized.push({...account,balance_cents:balance,equity_cents:equity,floating_profit_cents:floatingCents,account_size_cents:initial,account_profile:{accountType,fundingModel:direct?'Direct Funded':warrior?'Warrior':'Challenge',phase,model:account.challenge_model||null,isDirectFunded:direct,isWarrior:warrior,rules:{maxTradesPerDay:maxTrades,tradesToday,openPositions:openTrades.length,maxOpenPositions:1,consistencyLimitPercent:consistencyLimit==null?null:Number(consistencyLimit),consistencyAchievedPercent:Number(consistencyCurrent.toFixed(2)),consistencyMet:consistencyLimit==null||consistencyCurrent<=Number(consistencyLimit),dailyDrawdownPercent:dailyBps==null?null:dailyBps/100,dailyDrawdownCents:dailyLimit,dailyDrawdownUsedCents:dailyUsed,dailyDrawdownRemainingCents:dailyLimit==null?null:Math.max(0,dailyLimit-dailyUsed),maximumDrawdownPercent:maxBps==null?null:maxBps/100,maxDrawdownCents:maxLimit,maxDrawdownUsedCents:maxUsed,maxDrawdownRemainingCents:maxLimit==null?null:Math.max(0,maxLimit-maxUsed),profitTargetPercent:targetBps!=null?targetBps/100:targetPct,profitTargetCents:targetCents,profitAchievedCents:Math.max(0,balance-initial),targetProgressPercent:targetCents>0?Math.max(0,Math.min(100,(balance-initial)/targetCents*100)):null}})}res.json({accounts:normalized})}catch(error){res.status(500).json({error:error.message})}});`;
    return value.replace(/app\.get\('\/api\/accounts',authenticateToken,async\(req,res\)=>\{[\s\S]*?\n?\}\);/, route);
  }
  return value;
};
