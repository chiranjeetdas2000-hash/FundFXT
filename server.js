// FundFXT Render compatibility entrypoint + production trade repairs.
const fs=require('fs'),path=require('path');
const target=path.join(__dirname,'backend','server.js');let source=fs.readFileSync(target,'utf8');
const fixedTradeQuery="        const [trades] = await db.execute(\"SELECT * FROM trades WHERE trade_id = ? AND user_id = ? AND status = 'OPEN'\", [req.params.tradeId, req.userId]);";
source=source.split('\n').map(line=>line.includes('req.params.tradeId')&&line.includes('const [trades]')?fixedTradeQuery:line).join('\n');
source=source.replace(/await db\.execute\('UPDATE accounts SET balance_cents = balance_cents \+ \? WHERE id = \?', \[(trade|account)\.account_id\]\);/g,"await db.execute('UPDATE accounts SET balance_cents = balance_cents + ?, equity_cents = equity_cents + ? WHERE id = ?', [realizedCents, realizedCents, $1.account_id]);");
source=source.replace("status === 'PAYMENT_DONE' || status === 'PAYMENT_APPROVED'","status === 'PAYMENT_DONE' || status === 'PAYMENT_APPROVED' || status === 'ACCOUNT_CREATED' || status === 'ACCOUNT_PROVIDED'");
const groupedPL=`function calculatePL(symbol,side,entry,current,volume){const inst=instruments[symbol];if(!inst)throw new Error('Unsupported symbol: '+symbol);const pair=String(symbol).toUpperCase(),quote=pair.slice(3,6);const signedMove=String(side).toUpperCase()==='BUY'?Number(current)-Number(entry):Number(entry)-Number(current);const quotePL=signedMove*Number(inst.size)*Number(volume);if(quote==='USD')return quotePL;const prices=global.prices||{},direct=prices[quote+'USD'],inverse=prices['USD'+quote];let usdPerQuote=0;if(direct){const px=Number(direct.mid||direct.bid||direct.ask);if(Number.isFinite(px)&&px>0)usdPerQuote=px}else if(inverse){const px=Number(inverse.mid||inverse.bid||inverse.ask);if(Number.isFinite(px)&&px>0)usdPerQuote=1/px}if(!usdPerQuote)throw new Error('USD conversion quote unavailable for '+quote);return quotePL*usdPerQuote;}`;
source=source.replace(/function calculatePL\(symbol, side, entry, current, volume\) \{[\s\S]*?\n\}/,groupedPL);
const accountRoute=`app.get('/api/accounts',authenticateToken,async(req,res)=>{try{const[accounts]=await db.execute('SELECT * FROM accounts WHERE user_id = ?',[req.userId]);const normalized=[];for(const account of accounts){const model=String(account.challenge_model||'').toLowerCase();let config={};try{const[cfg]=await db.execute('SELECT * FROM challenge_configs WHERE model_key = ? LIMIT 1',[model]);config=cfg[0]||{}}catch(_){}const num=v=>v===null||v===undefined||v===''?null:Number(v),pick=(...keys)=>{for(const k of keys)if(config[k]!==null&&config[k]!==undefined&&config[k]!=='')return config[k];return null},accountPick=(...keys)=>{for(const k of keys)if(account[k]!==null&&account[k]!==undefined&&account[k]!=='')return account[k];return null};const warrior=model==='warrior_5k',direct=model==='prototype_5k',initial=num(account.initial_balance_cents)??num(account.balance_cents)??0,balance=num(account.balance_cents)??0,equity=num(account.equity_cents)??balance,dayStart=num(account.day_start_balance_cents)??balance,hwm=num(account.equity_hwm_cents)??initial;const phaseRaw=account.phase||account.account_phase||account.challenge_phase||(warrior?'Phase 1':direct?'Direct Funded':'—'),phase=String(phaseRaw);const phase2=/2/.test(phase);const targetCents=num(accountPick('profit_target_cents','target_profit_cents','target_cents'))??num(pick('profit_target_cents','target_profit_cents','target_cents'));const targetBps=num(pick(phase2?'phase2_profit_target_bps':'phase1_profit_target_bps',phase2?'profit_target_phase2_bps':'profit_target_phase1_bps','profit_target_bps','target_bps','profit_target_percent_bps'));const targetPercent=targetBps==null?(phase2?(warrior?5:null):(warrior?8:null)):targetBps/100;const resolvedTargetCents=targetCents!=null?targetCents:(targetPercent!=null?Math.round(initial*targetPercent/100):null);const consistencyBps=num(pick('consistency_bps','consistency_rule_bps','consistency_percent_bps'));const consistencyLimit=num(accountPick('consistency_limit_percent'))??(consistencyBps==null?(direct?35:null):consistencyBps/100);const maxTrades=num(accountPick('max_trades_per_day','max_daily_trades'))??num(pick('max_trades_per_day','max_daily_trades'))??3;const minLot=num(accountPick('min_lot','min_lot_size'))??num(pick('min_lot','min_lot_size'))??0.01;const maxLot=num(accountPick('max_lot','max_lot_size'))??num(pick('max_lot','max_lot_size'))??2;const payoutDays=num(accountPick('payout_period_days','payout_days'))??num(pick('payout_period_days','payout_days'))??7;const dailyDDbps=num(accountPick('daily_dd_bps'))??num(config.daily_dd_bps),maxDDbps=num(accountPick('max_dd_bps'))??num(config.max_dd_bps),dailyLimitCents=dailyDDbps==null?null:Math.round(dayStart*dailyDDbps/10000),dailyUsedCents=Math.max(0,dayStart-balance),maxLimitCents=maxDDbps==null?null:Math.round(hwm*maxDDbps/10000),maxUsedCents=maxDDbps==null?0:Math.max(0,hwm-equity);let consistencyAchieved=0;try{const[days]=await db.execute("SELECT trading_day,SUM(CASE WHEN realized_profit_cents>0 THEN realized_profit_cents ELSE 0 END) AS day_profit FROM trades WHERE account_id=? AND status='CLOSED' GROUP BY trading_day",[account.id]);const profits=days.map(r=>Number(r.day_profit||0)).filter(v=>v>0),totalProfit=profits.reduce((s,v)=>s+v,0);if(totalProfit>0)consistencyAchieved=Math.max(...profits)/totalProfit*100}catch(_){}const[todayRows]=await db.execute("SELECT COUNT(*) AS count FROM trades WHERE account_id=? AND trading_day=CURDATE()",[account.id]);const[openRows]=await db.execute("SELECT COUNT(*) AS count FROM trades WHERE account_id=? AND status='OPEN'",[account.id]);const profitAchievedCents=Math.max(0,balance-initial),targetProgress=resolvedTargetCents&&resolvedTargetCents>0?Math.max(0,Math.min(100,profitAchievedCents/resolvedTargetCents*100)):null,accountType=account.account_type||(warrior?'Warrior Account':direct?'Direct Funded Account':'Challenge Account');normalized.push({...account,account_profile:{accountType,fundingModel:direct?'Direct Funded':warrior?'Warrior':'Challenge',phase,model:account.challenge_model||null,isDirectFunded:direct,isWarrior:warrior,rules:{maxTradesPerDay:maxTrades,tradesToday:Number(todayRows[0].count||0),openPositions:Number(openRows[0].count||0),maxOpenPositions:1,minLot,maxLot,payoutPeriodDays:payoutDays,consistencyLimitPercent:consistencyLimit,consistencyAchievedPercent:Number(consistencyAchieved.toFixed(2)),consistencyMet:consistencyLimit==null||consistencyAchieved<=consistencyLimit,dailyDrawdownPercent:dailyDDbps==null?null:dailyDDbps/100,dailyDrawdownCents:dailyLimitCents,dailyDrawdownUsedCents:dailyUsedCents,dailyDrawdownRemainingCents:dailyLimitCents==null?null:Math.max(0,dailyLimitCents-dailyUsedCents),maximumDrawdownPercent:maxDDbps==null?null:maxDDbps/100,maxDrawdownPercent:maxDDbps==null?null:maxDDbps/100,maxDrawdownCents:maxLimitCents,maxDrawdownUsedCents:maxUsedCents,maxDrawdownRemainingCents:maxLimitCents==null?null:Math.max(0,maxLimitCents-maxUsedCents),profitTargetPercent:targetPercent,profitTargetCents:resolvedTargetCents,profitAchievedCents,targetProgressPercent:targetProgress}})}res.json({accounts:normalized})}catch(error){res.status(500).json({error:error.message})}});`;
source=source.replace(/app\.get\('\/api\/accounts',authenticateToken,async\(req,res\)=>\{[\s\S]*?\n\}\);/,accountRoute);
const favoriteFlow=`\n(async()=>{try{await db.execute("CREATE TABLE IF NOT EXISTS account_favorite_pairs (id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY, account_id BIGINT NOT NULL, symbol VARCHAR(20) NOT NULL, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, UNIQUE KEY uq_account_symbol (account_id,symbol), KEY idx_account_favorites (account_id)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4")}catch(e){console.error('Favorite-pair table init:',e.message)}})();\napp.get('/api/favorite-pairs',authenticateToken,async(req,res)=>{try{const[a]=await db.execute('SELECT id,account_code FROM accounts WHERE user_id=?',[req.userId]);const account=a.find(x=>String(x.account_code)===String(req.query.account_code))||a[0];if(!account)return res.status(404).json({error:'No trading account available'});const[rows]=await db.execute('SELECT symbol FROM account_favorite_pairs WHERE account_id=? ORDER BY id ASC',[account.id]);res.json({success:true,account_code:account.account_code,favorites:rows.map(r=>r.symbol)})}catch(e){res.status(500).json({error:e.message})}});\napp.put('/api/favorite-pairs',authenticateToken,async(req,res)=>{try{const[a]=await db.execute('SELECT id,account_code FROM accounts WHERE user_id=?',[req.userId]);const account=a.find(x=>String(x.account_code)===String(req.body.account_code))||a[0];if(!account)return res.status(404).json({error:'No trading account available'});const favorites=Array.isArray(req.body.favorites)?[...new Set(req.body.favorites.map(x=>String(x).toUpperCase()).filter(Boolean))].slice(0,50):[];await db.execute('DELETE FROM account_favorite_pairs WHERE account_id=?',[account.id]);for(const symbol of favorites)await db.execute('INSERT IGNORE INTO account_favorite_pairs(account_id,symbol) VALUES(?,?)',[account.id,symbol]);res.json({success:true,account_code:account.account_code,favorites})}catch(e){res.status(500).json({error:e.message})}});\n`;
const affiliateFlow=`\napp.get('/api/affiliate/stats',authenticateToken,async(req,res)=>{try{const[users]=await db.execute('SELECT affiliate_code FROM users WHERE id=? LIMIT 1',[req.userId]);if(!users.length)return res.status(404).json({error:'User not found'});const code=users[0].affiliate_code;const[refRows]=await db.execute('SELECT COUNT(*) AS n FROM users WHERE referred_by_code=?',[code]);const[salesRows]=await db.execute("SELECT COUNT(*) AS n FROM affiliate_commissions WHERE affiliate_id=(SELECT id FROM affiliates WHERE user_id=? LIMIT 1)",[req.userId]);const[earnedRows]=await db.execute("SELECT COALESCE(SUM(commission_amount_cents),0) AS total FROM affiliate_commissions WHERE affiliate_id=(SELECT id FROM affiliates WHERE user_id=? LIMIT 1) AND status IN ('PENDING','APPROVED','PAID')",[req.userId]);const[pendingRows]=await db.execute("SELECT COALESCE(SUM(commission_amount_cents),0) AS total FROM affiliate_commissions WHERE affiliate_id=(SELECT id FROM affiliates WHERE user_id=? LIMIT 1) AND status='PENDING'",[req.userId]);res.json({success:true,total_referrals:Number(refRows[0]?.n||0),total_sales:Number(salesRows[0]?.n||0),total_earnings_cents:Number(earnedRows[0]?.total||0),pending_earnings_cents:Number(pendingRows[0]?.total||0)})}catch(e){res.status(500).json({error:e.message})}});\napp.get('/api/affiliate/dashboard',authenticateToken,async(req,res)=>{try{const[rows]=await db.execute('SELECT id,affiliate_code FROM users WHERE id=? LIMIT 1',[req.userId]);if(!rows.length)return res.status(404).json({error:'User not found'});const[commissions]=await db.execute('SELECT c.id,c.order_ref,c.model,c.paid_amount_cents,c.commission_amount_cents AS commission_cents,c.status,c.created_at,u.legal_name AS customer_name FROM affiliate_commissions c LEFT JOIN users u ON u.id=c.referred_user_id WHERE c.affiliate_id=(SELECT id FROM affiliates WHERE user_id=? LIMIT 1) ORDER BY c.created_at DESC',[req.userId]);const totals=commissions.reduce((a,c)=>{const v=Number(c.commission_cents||0);a.total+=v;if(String(c.status).toUpperCase()==='PENDING')a.pending+=v;return a},{total:0,pending:0});res.json({success:true,affiliate_code:rows[0].affiliate_code||null,total_earnings_cents:totals.total,available_earnings_cents:totals.pending,verified_sales:commissions.length,commissions})}catch(e){res.status(500).json({error:e.message})}});\n`;
const marker='// ========== WEBSOCKET SERVER ==========';
source=source.replace(marker,affiliateFlow+favoriteFlow+marker);
if(!source.includes("/api/trades/:tradeId/partial-close")){source=source.replace(marker,`app.post('/api/trades/:tradeId/partial-close',authenticateToken,async(req,res)=>{try{const closeVolume=Number(req.body.volume);if(!Number.isFinite(closeVolume)||closeVolume<=0)return res.status(400).json({error:'Invalid close volume'});const[rows]=await db.execute("SELECT * FROM trades WHERE trade_id=? AND user_id=? AND status='OPEN'",[req.params.tradeId,req.userId]);if(!rows.length)return res.status(404).json({error:'Open trade not found'});const trade=rows[0],originalVolume=Number(trade.volume);if(closeVolume>=originalVolume)return res.status(400).json({error:'Use full close for the complete position'});const price=global.priceCache?.[trade.symbol];if(!price)return res.status(503).json({error:'Live price unavailable'});const exitPrice=trade.side==='BUY'?Number(price.bid):Number(price.ask),realizedCents=Math.round(calculatePL(trade.symbol,trade.side,Number(trade.entry_price),exitPrice,closeVolume)*100),remaining=Math.round((originalVolume-closeVolume)*100)/100;await db.execute('UPDATE trades SET volume=?,current_price=?,floating_profit_cents=0 WHERE trade_id=?',[remaining,exitPrice,trade.trade_id]);const partialId='TRP-'+Date.now().toString(36).toUpperCase();await db.execute("INSERT INTO trades (trade_id,account_id,account_code,user_id,symbol,side,volume,entry_price,entry_time,trading_day,stop_loss,take_profit,status,exit_price,exit_time,realized_profit_cents,close_reason) VALUES (?,?,?,?,?,?,?, ?, ?,CURDATE(),?,?, 'CLOSED', ?, NOW(), ?, 'MANUAL_PARTIAL')",[partialId,trade.account_id,trade.account_code,trade.user_id,trade.symbol,trade.side,closeVolume,trade.entry_price,trade.entry_time,trade.stop_loss,trade.take_profit,exitPrice,realizedCents]);await db.execute('UPDATE accounts SET balance_cents=balance_cents+?,equity_cents=equity_cents+? WHERE id=?',[realizedCents,realizedCents,trade.account_id]);res.json({success:true,trade_id:partialId,remaining_volume:remaining,exit_price:exitPrice,realized_profit:realizedCents/100})}catch(error){res.status(500).json({error:error.message})}});\n\n`+marker)}
if(!source.includes("/api/trades/:tradeId/modify")){source=source.replace(marker,`app.post('/api/trades/:tradeId/modify',authenticateToken,async(req,res)=>{try{const[rows]=await db.execute("SELECT * FROM trades WHERE trade_id=? AND user_id=? AND status='OPEN'",[req.params.tradeId,req.userId]);if(!rows.length)return res.status(404).json({error:'Open trade not found'});const trade=rows[0],side=String(trade.side).toUpperCase(),entry=Number(trade.entry_price),hasTP=Object.prototype.hasOwnProperty.call(req.body,'tp'),hasSL=Object.prototype.hasOwnProperty.call(req.body,'sl'),tp=hasTP?(req.body.tp===null||req.body.tp===''?null:Number(req.body.tp)):trade.take_profit,sl=hasSL?(req.body.sl===null||req.body.sl===''?null:Number(req.body.sl)):trade.stop_loss;if((tp!==null&&!Number.isFinite(tp))||(sl!==null&&!Number.isFinite(sl)))return res.status(400).json({error:'Invalid TP or SL price'});if(sl!==null&&((side==='BUY'&&sl>=entry)||(side==='SELL'&&sl<=entry)))return res.status(400).json({error:'Invalid Stop Loss for this direction'});if(tp!==null&&((side==='BUY'&&tp<=entry)||(side==='SELL'&&tp>=entry)))return res.status(400).json({error:'Invalid Take Profit for this direction'});await db.execute('UPDATE trades SET stop_loss=?,take_profit=? WHERE trade_id=?',[sl,tp,trade.trade_id]);res.json({success:true,trade_id:trade.trade_id,stop_loss:sl,take_profit:tp})}catch(error){res.status(500).json({error:error.message})}});\n\n`+marker)}
fs.writeFileSync(target,source,'utf8');
console.log('FundFXT: database-driven account rules + persistent account favorites + affiliate commission flow + grouped USD P&L + partial close + TP/SL repairs applied before startup.');
require('./backend/server.js');
setTimeout(async()=>{try{const{reconcileAffiliateCommissions}=require('./backend/affiliate-reconciliation');const result=await reconcileAffiliateCommissions();console.log('FundFXT affiliate reconciliation:',JSON.stringify(result))}catch(e){console.error('FundFXT affiliate reconciliation startup error:',e.message)}},5000);
