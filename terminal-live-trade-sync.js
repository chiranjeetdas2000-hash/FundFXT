'use strict';
(function(){
  const API='https://fundfxt.onrender.com';let account=null,tab='OPEN',timer=null,busy=false;
  const $=id=>document.getElementById(id),headers=()=>({Authorization:`Bearer ${localStorage.getItem('fundfxt_token')||''}`,Accept:'application/json'});
  const esc=v=>String(v??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]||m));
  const fmt=v=>v==null||v===''||!Number.isFinite(Number(v))?'—':Number(v).toFixed(/JPY$/i.test(arguments[1])?3:2);
  const price=(v,s)=>v==null||v===''||!Number.isFinite(Number(v))?'—':Number(v).toFixed(/JPY$/i.test(s)?3:/XAU|XAG|BTC|ETH/i.test(s)?2:5);
  const money=v=>'$'+(Number(v||0)/100).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
  async function get(path,opt={}){const r=await fetch(API+path,{...opt,headers:{...headers(),...(opt.headers||{})}});let d={};try{d=await r.json()}catch{}if(!r.ok)throw Error(d.error||`HTTP ${r.status}`);return d}
  async function resolve(){const p=new URLSearchParams(location.search),code=p.get('account_code')||p.get('account')||localStorage.getItem('fundfxt_selected_account')||'',d=await get('/api/accounts'),list=Array.isArray(d)?d:(d.accounts||[]),a=(code&&list.find(x=>String(x.account_code)===String(code)))||list[0];if(!a)throw Error('No trading account available');account=a;localStorage.setItem('fundfxt_selected_account',a.account_code);if($('rightAccount'))$('rightAccount').textContent=a.account_code}
  function accountView(a){if(!a)return;if($('balance'))$('balance').textContent=money(a.balance_cents);if($('equity'))$('equity').textContent=money(a.equity_cents);if($('accountStatus'))$('accountStatus').textContent=`${a.status||'ACTIVE'} · ${a.account_code||''}`}
  function pnl(t){if(t.status==='CLOSED')return Number(t.realized_profit_cents||0)/100;const c=Number(t.current_price),e=Number(t.entry_price),v=Number(t.volume),contract=/XAU|XAG/i.test(t.symbol)?100:/BTC|ETH/i.test(t.symbol)?1:100000;return Number.isFinite(c)&&Number.isFinite(e)&&Number.isFinite(v)?(t.side==='BUY'?c-e:e-c)*v*contract:0}
  function render(trades){const box=$('tradeScroll');if(!box)return;const rows=trades.filter(t=>tab==='OPEN'?t.status==='OPEN':tab==='PENDING'?t.status==='PENDING':t.status==='CLOSED');if($('tradeCount'))$('tradeCount').textContent=trades.filter(t=>t.status==='OPEN').length;box.innerHTML=rows.length?rows.map(t=>{const p=pnl(t),actions=t.status==='OPEN'?`<button class="mini" data-live-mod="${esc(t.trade_id)}">SL/TP</button><button class="mini close" data-live-close="${esc(t.trade_id)}">Close</button>`:t.status==='PENDING'?`<button class="mini close" data-live-cancel="${esc(t.trade_id)}">Cancel</button>`:'';return `<article class="trade-card premium-trade-card"><div class="premium-trade-head"><div class="premium-pair-block"><div class="premium-pair-row"><b>${esc(t.symbol)}</b><span class="premium-direction ${t.side==='SELL'?'sell':''}">${esc(t.side)}</span><span class="premium-lot">${esc(t.volume)} LOT</span></div><small>ORDER ID · ${esc(t.trade_id)}</small></div></div><div class="premium-entry-row"><div><span>ENTRY PRICE</span><b>${price(t.entry_price,t.symbol)}</b></div><div class="premium-entry-action">${t.status==='OPEN'?`<button class="mini premium-sltp" data-live-mod="${esc(t.trade_id)}">SL / TP</button>`:''}</div></div><div class="premium-live-row"><div><span>${t.status==='PENDING'?'STATUS':'CURRENT'}</span><b>${t.status==='PENDING'?'PENDING':price(t.current_price,t.symbol)}</b></div><div class="premium-pnl ${p<0?'negative':'positive'}"><span>${t.status==='CLOSED'?'REALIZED P/L':'P / L'}</span><b>${p>=0?'+':''}$${p.toFixed(2)}</b></div></div><div class="trade-buttons premium-buttons">${actions}</div></article>`}).join(''):`<div class="empty"><strong>No ${tab.toLowerCase()} trades</strong><span>${tab==='OPEN'?'Open positions for this account will appear here.':tab==='PENDING'?'Pending orders for this account will appear here.':'Closed trades for this account will appear here.'}</span></div>`;box.querySelectorAll('[data-live-close]').forEach(b=>b.onclick=async()=>{try{await get('/api/trades/'+encodeURIComponent(b.dataset.liveClose)+'/close',{method:'POST',body:'{}'});await sync()}catch(e){window.toast?.(e.message)}});box.querySelectorAll('[data-live-cancel]').forEach(b=>b.onclick=async()=>{try{await get('/api/trades/'+encodeURIComponent(b.dataset.liveCancel),{method:'DELETE'});await sync()}catch(e){window.toast?.(e.message)}});box.querySelectorAll('[data-live-mod]').forEach(b=>b.onclick=()=>window.modifyTrade?.(b.dataset.liveMod))}
  async function sync(){if(busy||!account)return;busy=true;try{const[s,t]=await Promise.all([get('/api/accounts/'+encodeURIComponent(account.id)+'/summary'),get('/api/accounts/'+encodeURIComponent(account.id)+'/trades')]);accountView(s.account||s);render(Array.isArray(t.trades)?t.trades:[])}catch(e){console.warn('FundFXT authoritative sync:',e.message)}finally{busy=false}}
  function setup(){document.querySelectorAll('.right-tab').forEach(b=>b.onclick=()=>{tab=b.dataset.tab||'OPEN';document.querySelectorAll('.right-tab').forEach(x=>x.classList.toggle('active',x===b));sync()});resolve().then(()=>{sync();timer=setInterval(sync,1000)}).catch(e=>console.warn('FundFXT account sync:',e.message))}
  document.addEventListener('DOMContentLoaded',setup);
})();
