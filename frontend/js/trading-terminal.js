'use strict';
/* ===== TRADING TERMINAL CONTROLLER ===== */
const FX_API='https://fundfxt.onrender.com';
const SYMBOLS=['EURUSD','GBPUSD','USDJPY','USDCHF','AUDUSD','USDCAD','NZDUSD','EURGBP','EURJPY','EURAUD','EURCHF','EURNZD','GBPJPY','GBPCHF','GBPAUD','GBPNZD','AUDJPY','AUDNZD','AUDCAD','AUDCHF','CADJPY','CADCHF','CHFJPY','NZDJPY','NZDCHF','NZDCAD','XAUUSD','XAGUSD'];
const T={account:null,prices:{},selected:'EURUSD',tab:'OPEN',favorites:[],trades:[]};
const $=id=>document.getElementById(id);const token=()=>localStorage.getItem('fundfxt_token')||'';
const money=c=>'$'+(Number(c||0)/100).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
const fmt=(v,s)=>Number.isFinite(Number(v))?Number(v).toFixed(/JPY$/i.test(s)?3:/XAU|XAG|BTC|ETH/i.test(s)?2:5):'—';
async function api(path,opt={}){const r=await fetch(FX_API+path,{...opt,headers:{Authorization:'Bearer '+token(),'Content-Type':'application/json',...(opt.headers||{})}});let d={};try{d=await r.json()}catch{}if(!r.ok)throw Error(d.error||d.message||`Request failed (${r.status})`);return d}
function feedback(msg,ok=false){const x=$('tradeFeedback');if(!x)return;x.textContent=msg;x.className='trade-feedback show '+(ok?'ok':'error');clearTimeout(feedback.t);feedback.t=setTimeout(()=>x.className='trade-feedback',5000)}
function loadFavorites(){try{T.favorites=JSON.parse(localStorage.getItem('fundfxt_favorite_pairs')||'[]').filter(s=>SYMBOLS.includes(s))}catch{T.favorites=[]}if(!T.favorites.length)T.favorites=SYMBOLS.slice(0,5);localStorage.setItem('fundfxt_favorite_pairs',JSON.stringify(T.favorites))}
function saveFavorites(){localStorage.setItem('fundfxt_favorite_pairs',JSON.stringify(T.favorites))}
function renderPairs(){const box=$('watchlist');if(!box)return;const q=($('search')?.value||'').toUpperCase().trim();const rows=T.favorites.filter(s=>s.includes(q));box.innerHTML=rows.map(s=>{const p=T.prices[s]||{},mid=Number(p.mid),spr=Number.isFinite(Number(p.spread))?Number(p.spread):(Number(p.ask)-Number(p.bid)),ch=Number(p.changePercent);return `<button class="quote ${s===T.selected?'selected':''}" data-symbol="${s}" type="button"><span class="quote-main"><b class="watch-symbol">${s}</b><span class="quote-change">Δ ${Number.isFinite(ch)?(ch>=0?'+':'')+ch.toFixed(2)+'%':'—'}</span></span><span class="quote-spread">S ${fmt(spr,s)}</span><span class="quote-mid"><small>MID</small><b>${fmt(mid,s)}</b></span></button>`}).join('')||'<div class="favorite-empty">No favorite pairs match your search.</div>';box.querySelectorAll('.quote').forEach(b=>b.onclick=()=>{T.selected=b.dataset.symbol;renderPairs();openPanel('center');loadChart()});$('market').textContent=Object.values(T.prices).some(p=>Number.isFinite(Number(p.bid))&&Number.isFinite(Number(p.ask)))?'● LIVE':'● OFFLINE';$('market').style.color=$('market').textContent.includes('LIVE')?'var(--green)':'var(--red)'}
async function loadPrices(){const d=await api('/api/prices');T.prices=d.prices||{};renderPairs()}
async function loadAccount(){const d=await api('/api/accounts');const list=d.accounts||d;const wanted=new URLSearchParams(location.search).get('account_code')||new URLSearchParams(location.search).get('account')||localStorage.getItem('fundfxt_selected_account');T.account=list.find(a=>String(a.account_code)===String(wanted))||list[0];if(!T.account)throw Error('No trading account available');localStorage.setItem('fundfxt_selected_account',T.account.account_code);$('rightAccount').textContent=T.account.account_code;renderAccount()}
function renderAccount(){if(T.account){$('balance').textContent=money(T.account.balance_cents);$('equity').textContent=money(T.account.equity_cents)}}
function loadChart(){const host=$('tv');if(!host)return;host.innerHTML='';const f=document.createElement('iframe');f.title='FundFXT '+T.selected;f.allowFullscreen=true;f.src='https://www.tradingview.com/widgetembed/?symbol='+encodeURIComponent('OANDA:'+T.selected)+'&interval=15&theme=dark&style=1&locale=en&enable_publishing=false&hide_top_toolbar=false&hide_side_toolbar=false&save_image=false&allow_symbol_change=true&autosize=true';host.appendChild(f)}
function openPanel(name){document.querySelectorAll('.panel').forEach(p=>p.classList.remove('mobile-active'));$(name)?.classList.add('mobile-active');document.querySelectorAll('.mobile-nav button').forEach(b=>b.classList.toggle('active',b.dataset.view===name));if(name==='right')loadTrades();if(name==='center')loadChart()}
function renderTradeCard(t){if(t.status==='CLOSED')return `<article class="trade-card history-card" data-details="${t.trade_id}"><div class="trade-main"><div><b class="trade-symbol">${t.symbol}</b><small class="trade-id">${t.trade_id}</small></div><b class="trade-side ${String(t.side).toLowerCase()}">${t.side}</b></div><div class="trade-meta"><div><span>Entry</span><b>${fmt(Number(t.entry_price),t.symbol)}</b></div><div><span>Lot</span><b>${Number(t.volume).toFixed(2)}</b></div><div><span>Exit</span><b>${fmt(Number(t.exit_price),t.symbol)}</b></div><div><span>Profit / Loss</span><b class="pl ${Number(t.realized_profit_cents||0)>=0?'green':'red'}">${Number(t.realized_profit_cents||0)>=0?'+':''}${money(t.realized_profit_cents)}</b></div></div><small class="muted">Tap for trade details</small></article>`;const p=T.prices[t.symbol]||{},cur=t.side==='BUY'?p.bid:p.ask,pl=Number.isFinite(cur)?(t.side==='BUY'?cur-Number(t.entry_price):Number(t.entry_price)-cur)*Number(t.volume)*(/XAU|GOLD/i.test(t.symbol)?100:100000):Number(t.floating_profit_cents||0)/100;return `<article class="trade-card"><div class="trade-main"><div><b class="trade-symbol">${t.symbol}</b><small class="trade-id">${t.trade_id}</small></div><b class="trade-side ${String(t.side).toLowerCase()}">${t.side} · ${Number(t.volume).toFixed(2)}L</b></div><div class="trade-meta"><div><span>Entry</span><b>${fmt(Number(t.entry_price),t.symbol)}</b></div><div><span>Current</span><b>${fmt(cur,t.symbol)}</b></div><div><span>P/L</span><b class="pl ${pl>=0?'green':'red'}">${pl>=0?'+':''}$${pl.toFixed(2)}</b></div></div><div class="trade-actions"><button class="mini" data-details="${t.trade_id}" type="button">Details</button><button class="mini close" data-close="${t.trade_id}" type="button">Close</button><button class="mini" data-partial="${t.trade_id}" type="button">Partial close</button></div></article>`}
function renderTrades(){const box=$('tradeScroll');if(!box)return;const rows=T.trades.filter(t=>t.status===T.tab);box.innerHTML=rows.map(renderTradeCard).join('')||`<div class="empty"><strong>No ${T.tab.toLowerCase()} trades</strong><span>${T.tab==='CLOSED'?'Closed trades will appear here.':'No positions available.'}</span></div>`;box.querySelectorAll('[data-details]').forEach(b=>b.onclick=()=>showDetails(b.dataset.details));box.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>closePosition(b.dataset.close,false));box.querySelectorAll('[data-partial]').forEach(b=>b.onclick=()=>partialClose(b.dataset.partial))}
async function loadTrades(){if(!T.account)return;try{const d=await api('/api/trade/get?account_code='+encodeURIComponent(T.account.account_code));T.trades=Array.isArray(d.trades)?d.trades:[];renderTrades()}catch(e){feedback(e.message)}}
function showDetails(id){const t=T.trades.find(x=>String(x.trade_id)===String(id));if(!t)return;const fields=[['Trade ID',t.trade_id],['Pair',t.symbol],['Direction',t.side],['Lot Size',Number(t.volume).toFixed(2)],['Open Time',t.entry_time||t.created_at||'—'],['Close Time',t.exit_time||'—'],['Entry Price',fmt(Number(t.entry_price),t.symbol)],['Exit Price',fmt(Number(t.exit_price),t.symbol)],['Take Profit',fmt(Number(t.take_profit),t.symbol)],['Stop Loss',fmt(Number(t.stop_loss),t.symbol)],['Exit By',t.close_reason||'—'],['Calculated P/L',money(t.realized_profit_cents)]].map(([a,b])=>`<div class="detail"><span>${a}</span><b>${b??'—'}</b></div>`).join('');modal('Trade Details',fields,'')}
function modal(title,body,actions){document.querySelector('.trade-modal')?.remove();const x=document.createElement('div');x.className='trade-modal';x.innerHTML=`<div class="trade-modal-backdrop"></div><div class="trade-modal-card"><div class="trade-modal-head"><h3>${title}</h3><button class="trade-modal-close" type="button">×</button></div><div class="detail-grid">${body}</div>${actions?`<div class="modal-actions">${actions}</div>`:''}</div>`;document.body.appendChild(x);x.querySelector('.trade-modal-close').onclick=()=>x.remove();x.querySelector('.trade-modal-backdrop').onclick=()=>x.remove()}
async function closePosition(id,partial){const t=T.trades.find(x=>String(x.trade_id)===String(id));if(!t)return;const amount=partial?Number(prompt(`Close how many lots? Max ${t.volume}`)):Number(t.volume);if(!Number.isFinite(amount)||amount<=0||amount>Number(t.volume))return feedback('Invalid close volume.');try{await api('/api/trades/'+encodeURIComponent(id)+'/close',{method:'POST',body:JSON.stringify({volume:amount})});feedback(partial?'Partial position closed successfully.':'Position closed successfully.',true);await loadAccount();await loadTrades()}catch(e){feedback(e.message)}}
function partialClose(id){closePosition(id,true)}
async function execute(side){const code=T.account?.account_code,volume=Number($('volume').value),sl=$('sl').value===''?null:Number($('sl').value),tp=$('tp').value===''?null:Number($('tp').value);if(!code)return feedback('No trading account selected.');if(!Number.isFinite(volume)||volume<.01||volume>2)return feedback('Lot size must be 0.01 to 2.00.');try{const d=await api('/api/trade/execute',{method:'POST',body:JSON.stringify({account_code:code,symbol:T.selected,side,volume,sl,tp})});feedback(`${side} ${T.selected} ${volume.toFixed(2)} lot executed successfully${d.trade_id?' · '+d.trade_id:''}`,true);await loadAccount();await loadTrades();openPanel('right')}catch(e){feedback(e.message)}}
function addPair(){const available=SYMBOLS.filter(s=>!T.favorites.includes(s));if(!available.length)return feedback('All supported pairs are already favorites.');modal('Add Favorite Pair',available.map(s=>`<div class="pair-add-item"><b>${s}</b><button data-add="${s}" type="button">Add</button></div>`).join(''),'');document.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>{T.favorites.push(b.dataset.add);saveFavorites();document.querySelector('.trade-modal')?.remove();renderPairs()})}
function logout(){localStorage.removeItem('fundfxt_token');localStorage.removeItem('fundfxt_selected_account');location.href='/dashboard.html'}
function setup(){loadFavorites();$('addPairBtn').onclick=addPair;$('search').oninput=renderPairs;$('buy').onclick=()=>execute('BUY');$('sell').onclick=()=>execute('SELL');$('orderType').onchange=()=>$('pendingFields').classList.toggle('hidden',$('orderType').value==='MARKET');$('refresh').onclick=async()=>{try{await loadPrices();await loadAccount();await loadTrades()}catch(e){feedback(e.message)}};document.querySelectorAll('.mobile-nav button').forEach(b=>b.onclick=()=>openPanel(b.dataset.view));document.querySelectorAll('.right-tab').forEach(b=>b.onclick=()=>{T.tab=b.dataset.tab;renderTrades()});$('chartMaxBtn').onclick=()=>{const c=$('center'),m=c.classList.toggle('chart-maximized');document.body.classList.toggle('chart-fullscreen',m);$('chartMaxBtn').textContent=m?'↙':'⛶'};$('chartBack').onclick=()=>openPanel('watch');$('tradesBack').onclick=()=>openPanel('center');$('accountMenuBtn').onclick=()=>modal('Account',`<div class="detail"><span>Account</span><b>${T.account?.account_code||'—'}</b></div><div class="detail"><span>Balance</span><b>${money(T.account?.balance_cents)}</b></div><div class="detail"><span>Equity</span><b>${money(T.account?.equity_cents)}</b></div>`,'<button class="danger" id="terminalLogout" type="button">Logout</button>');document.addEventListener('click',e=>{if(e.target.id==='terminalLogout')logout()});setInterval(()=>{loadPrices().catch(()=>{});if(T.account)loadTrades().catch(()=>{})},1000);loadAccount().then(loadPrices).then(()=>{renderPairs();loadChart();loadTrades()}).catch(e=>feedback(e.message));if(window.lucide?.createIcons)window.lucide.createIcons()}
document.addEventListener('DOMContentLoaded',setup);
