/* ===== FUNDFXT TERMINAL FIXES — ACCOUNT RULES + LOADING FEEDBACK ===== */
(function(){
'use strict';
const API='https://fundfxt.onrender.com';
const token=()=>localStorage.getItem('fundfxt_token')||'';
const num=v=>Number.isFinite(Number(v))?Number(v):0;
const money=c=>'$'+(num(c)/100).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
const pct=v=>v==null||!Number.isFinite(Number(v))?'—':num(v).toFixed(1)+'%';
const esc=v=>String(v??'—').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
async function rawJson(path,opt={}){const r=await fetch(API+path,{...opt,headers:{Authorization:'Bearer '+token(),'Content-Type':'application/json',...(opt.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Request failed');return d}
function selectedAccount(list){const q=new URLSearchParams(location.search);const wanted=q.get('account_code')||q.get('account')||localStorage.getItem('fundfxt_selected_account');return list.find(a=>String(a.account_code)===String(wanted))||list[0]||null}
async function tradesFor(account){if(!account)return [];try{const d=await rawJson('/api/trade/get?account_code='+encodeURIComponent(account.account_code));return Array.isArray(d.trades)?d.trades:[]}catch{return []}}
function ledger(account,trades){const initial=Math.round(num(account.initial_balance_cents||account.balance_cents));const closed=trades.filter(t=>String(t.status).toUpperCase()==='CLOSED');const open=trades.filter(t=>String(t.status).toUpperCase()==='OPEN');const realized=Math.round(closed.reduce((s,t)=>s+num(t.realized_profit_cents),0));const floating=Math.round(open.reduce((s,t)=>s+num(t.floating_profit_cents),0));return{initial,balance:initial+realized,equity:initial+realized+floating,realized,floating,openCount:open.length}}
function accountRules(a,trades){const p=a.account_profile||{},r=p.rules||{},x=ledger(a,trades);const warrior=!!p.isWarrior,direct=!!p.isDirectFunded,phase=String(p.phase||a.phase||a.account_phase||'—'),phase2=/2/.test(phase),type=p.accountType||'Trading Account',funding=p.fundingModel||'—',model=p.model||a.challenge_model||'—';const dailyLimit=r.dailyDrawdownCents!=null?num(r.dailyDrawdownCents):null;const dailyUsed=Math.max(0,Math.round(num(a.day_start_balance_cents||x.initial)-x.balance));const maxLimit=r.maxDrawdownCents!=null?num(r.maxDrawdownCents):(warrior?Math.round(x.initial*.08):null);const maxUsed=Math.max(0,x.initial-x.equity);const maxTrades=r.maxTradesPerDay!=null?num(r.maxTradesPerDay):(warrior?3:null);const tradesToday=r.tradesToday!=null?num(r.tradesToday):trades.filter(t=>String(t.trading_day||t.entry_time||t.exit_time||'').slice(0,10)===new Date().toISOString().slice(0,10)).length;const consistencyLimit=r.consistencyLimitPercent!=null?num(r.consistencyLimitPercent):(warrior?30:(direct?30:null));const consistency=r.consistencyAchievedPercent!=null?num(r.consistencyAchievedPercent):0;const phase1Cents=warrior?(r.phase1TargetCents!=null?num(r.phase1TargetCents):Math.round(x.initial*.08)):null;const phase2Cents=warrior?(r.phase2TargetCents!=null?num(r.phase2TargetCents):Math.round(x.initial*.05)):null;const targetCents=r.profitTargetCents!=null?num(r.profitTargetCents):(warrior?(phase2?phase2Cents:phase1Cents):null);const targetPercent=r.profitTargetPercent!=null?num(r.profitTargetPercent):(warrior?(phase2?5:8):null);const targetProgress=targetCents&&targetCents>0?Math.max(0,Math.min(100,(x.balance-x.initial)/targetCents*100)):null;const rule=(label,value)=>`<div class="rule"><span>${label}</span><b>${value}</b></div>`;const bar=(label,value,danger=false)=>{const v=Math.max(0,Math.min(100,num(value)));return `<div class="rule-label"><span>${label}</span><b>${pct(value)}</b></div><div class="rule-progress${danger?' rule-danger':''}"><i style="width:${v}%"></i></div>`};return `<div class="account-rules account-rules-detailed"><div class="account-rule-model"><b>${esc(type)}</b><span>${esc(funding)} · ${esc(phase)}</span></div><div class="rule-grid">${rule('Account type',esc(type))}${rule('Funding model',esc(funding))}${rule('Phase / stage',esc(phase))}${rule('Challenge model',esc(model))}${rule('Direct funded',direct?'Yes':'No')}${rule('Account size',money(x.initial))}${rule('Initial balance',money(x.initial))}${rule('Current balance',`<span id="accountRuleBalance">${money(x.balance)}</span>`)}${rule('Current equity',`<span id="accountRuleEquity">${money(x.equity)}</span>`)}${rule('Realized P/L',`${x.realized>=0?'+':''}${money(x.realized)}`)}${rule('Floating P/L',`${x.floating>=0?'+':''}${money(x.floating)}`)}${warrior?rule('Phase 1 profit target','8% · '+money(phase1Cents)):''}${warrior?rule('Phase 2 profit target','5% · '+money(phase2Cents)):''}${rule('Current phase target',targetCents!=null?`${targetPercent!=null?targetPercent+'% · ':''}${money(targetCents)}`:(direct?'No profit target':'Not configured'))}${dailyLimit!=null?rule('Daily drawdown limit',`${r.dailyDrawdownPercent!=null?num(r.dailyDrawdownPercent)+'% · ':''}${money(dailyLimit)}`):''}${maxLimit!=null?rule('Maximum drawdown limit',`${warrior?'8% · ':''}${money(maxLimit)}`):''}${maxTrades!=null?rule('Trades today',`${tradesToday} / ${maxTrades}`):''}${rule('Open positions',`${x.openCount} / ${r.maxOpenPositions!=null?num(r.maxOpenPositions):1}`)}${consistencyLimit!=null?rule('Consistency limit',`≤ ${pct(consistencyLimit)}`)+rule('Consistency achieved',pct(consistency)):''}</div>${targetCents!=null?bar('Current phase profit target progress',targetProgress):''}${dailyLimit!=null?bar('Daily drawdown used',dailyLimit?Math.min(100,dailyUsed/dailyLimit*100):0,true):''}${maxLimit!=null?bar('Maximum drawdown used',maxLimit?Math.min(100,maxUsed/maxLimit*100):0,true):''}${maxTrades!=null?bar('Daily trades used',maxTrades?tradesToday/maxTrades*100:0,true):''}${consistencyLimit!=null?bar('Consistency usage',consistencyLimit?consistency/consistencyLimit*100:0,true):''}${direct?'<div class="rule-note"><b>Direct funded rules:</b> no profit target · 30% consistency limit · maximum payout 100% one-time.</div>':''}${warrior?'<div class="rule-note"><b>Warrior rules:</b> Phase 1 target 8% · Phase 2 target 5% · maximum 3 trades per day · maximum drawdown 8% of initial balance.</div>':''}<button class="account-dashboard-btn" id="terminalAccountDashboard" type="button">View Full Account Dashboard <span>↗</span></button></div>`}
async function getAccount(){const d=await rawJson('/api/accounts'),list=d.accounts||[],a=selectedAccount(list);if(!a)throw Error('No trading account available');const t=await tradesFor(a);const x=ledger(a,t);return {...a,balance_cents:x.balance,equity_cents:x.equity,__trades:t}}
async function renderAccountPopover(){document.querySelector('.account-popover')?.remove();const shell=document.createElement('div');shell.className='account-popover account-loading';shell.innerHTML='<div class="account-loading-spinner"></div><div>Loading account overview…</div>';document.body.appendChild(shell);try{const a=await getAccount(),trades=a.__trades||[];localStorage.setItem('fundfxt_selected_account',a.account_code);shell.classList.remove('account-loading');shell.innerHTML=`<div class="account-popover-head"><div><h3>Account Overview</h3><div class="account-id">${esc(a.account_code)}</div></div><button class="account-popover-close" type="button" aria-label="Close">×</button></div><div class="account-values"><div class="account-value"><span>Balance</span><b id="accountPopupBalance">${money(a.balance_cents)}</b></div><div class="account-value"><span>Equity</span><b id="accountPopupEquity">${money(a.equity_cents)}</b></div></div>${accountRules(a,trades)}`;shell.querySelector('.account-popover-close').onclick=()=>shell.remove();shell.querySelector('#terminalAccountDashboard').onclick=()=>{shell.querySelector('#terminalAccountDashboard').disabled=true;location.href='/account-dashboard.html?account_code='+encodeURIComponent(a.account_code)};}catch(err){shell.classList.remove('account-loading');shell.innerHTML='<div class="account-error">'+esc(err.message||'Unable to load account')+'</div>'}}
async function refreshAccountPopover(){const p=document.querySelector('.account-popover');if(!p||p.classList.contains('account-loading'))return;try{const a=await getAccount(),trades=a.__trades||[];const b=p.querySelector('#accountPopupBalance'),e=p.querySelector('#accountPopupEquity');if(b)b.textContent=money(a.balance_cents);if(e)e.textContent=money(a.equity_cents);const mount=p.querySelector('.account-rules');if(mount)mount.outerHTML=accountRules(a,trades)}catch{}}
function setActiveTradeTab(tab){document.querySelectorAll('.right-tab').forEach(btn=>{const on=btn.dataset.tab===tab;btn.classList.toggle('active',on);btn.setAttribute('aria-selected',on?'true':'false')})}
function installLoadingFeedback(){if(document.getElementById('terminalLoadingFixStyle'))return;const s=document.createElement('style');s.id='terminalLoadingFixStyle';s.textContent=`.fx-busy{position:relative!important;pointer-events:none!important;opacity:.72!important}.fx-busy::after{content:'';width:12px;height:12px;border:2px solid currentColor;border-top-color:transparent;border-radius:50%;display:inline-block;margin-left:7px;vertical-align:-2px;animation:fxspin .7s linear infinite}.account-loading{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:12px;min-height:220px;color:#a8b0bc}.account-loading-spinner{width:32px;height:32px;border:3px solid #26303a;border-top-color:#00b56a;border-radius:50%;animation:fxspin .7s linear infinite}.account-error{padding:18px;color:#ff7b86}.account-dashboard-btn{width:100%;margin-top:13px;border:1px solid #00b56a;background:rgba(0,181,106,.09);color:#00d084;border-radius:9px;padding:11px 12px;font-weight:700;cursor:pointer}.account-dashboard-btn:disabled{opacity:.6}@keyframes fxspin{to{transform:rotate(360deg)}}`;document.head.appendChild(s);document.addEventListener('click',e=>{const btn=e.target.closest('button');if(!btn||btn.disabled||btn.matches('.right-tab,#accountMenuBtn,.account-popover-close,.account-dashboard-btn,.mobile-nav button,[data-no-busy]'))return;btn.classList.add('fx-busy');btn.setAttribute('aria-busy','true');setTimeout(()=>{if(document.body.contains(btn)){btn.classList.remove('fx-busy');btn.removeAttribute('aria-busy')}},7000)},true)}
function install(){installLoadingFeedback();const btn=document.getElementById('accountMenuBtn');if(btn)btn.onclick=async e=>{e.stopPropagation();if(document.querySelector('.account-popover'))document.querySelector('.account-popover').remove();else try{await renderAccountPopover()}catch(err){if(typeof feedback==='function')feedback(err.message)}};document.querySelectorAll('.right-tab').forEach(btn=>btn.addEventListener('click',()=>setActiveTradeTab(btn.dataset.tab),true));setActiveTradeTab('OPEN');document.addEventListener('click',e=>{const p=document.querySelector('.account-popover');if(p&&!p.contains(e.target)&&e.target!==btn)p.remove()});setInterval(refreshAccountPopover,1500)}
const wait=setInterval(()=>{if(document.readyState!=='loading'&&document.getElementById('accountMenuBtn')){clearInterval(wait);install()}},50);
})();